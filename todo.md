# tiling-verified todo

Ordered so each item's prerequisites appear earlier.  Each item must
compile cleanly before the next is begun.

## Repo hygiene and structure

1. Add `_CoqProject` and a `Makefile` generated by `coq_makefile`.
2. Add `README.md` listing the headline theorems, dependency story, and build instructions.
3. Add `.github/workflows/ci.yml` using `coq-community/docker-coq-action`, with `chmod -R a+rwX "$GITHUB_WORKSPACE"` before the Docker action and `coq_version: "9.0"` / `ocaml_version: "4.14-flambda"`.
4. Add `Examples.v` with three worked examples: consistency-of-PA from consistency-of-Q in the modal abstraction; Gödel's second incompleteness specialised to a concrete formula; an agent-tower with a non-cautious agent that uses `Bew (S n)` licensure.

## Predicate cures

5. Replace `NNIL_form := True` with the genuine "no nested implication under `Bot`" predicate; prove substitution closure on it.
6. Replace `Goldblatt_translation` (currently identity) with the standard `¬¬`-double-negation embedding plus modal box-rewriting; prove faithfulness.
7. Replace `Maehara_constructive_interp` (currently first projection) with a constructive interpolant by structural induction on a cut-free derivation.
8. Replace `single_modal_embed` (currently identity) with `forget_levels`-style level collapse with content.

## Tighten unused hypotheses

9. Tighten `sambin_uniqueness_at_higher_box_level` to consume its Sambin hypotheses on `psi1`, `psi2` via Sambin uniqueness on `phi` followed by `Nec`.
10. Tighten `agent_modal_T_kappa_correspondence` to derive its conclusion from the agent-tiling hypotheses.
11. Tighten `vingean_no_loebian_collapse` to derive consistency from the hypothesised tiling guarantee via `meta_consistency_no_contradiction` rather than the unconditional `meta_consistency_system`.
12. Tighten `licensure_implies_goal_preservation_for_cautious` so the agent action depends on `Hlic` and `Hcon`.
13. Audit the file for theorems whose `intros` lists hypotheses the proof body discards; consume each.

## Reflexive-existence cures

14. Replace `epsilon_0_contains_all_omega_towers : exists o, o = omega_tower n` with `forall n, ord_compare (omega_tower n) (omega_tower (S n)) = Lt`.
15. Replace `Gamma_0_approx_unbounded` with `forall k, exists o, ord_compare (Gamma_0_approx k) o = Lt`.
16. Replace `Gamma_0_strictly_above_epsilon_0_witnesses` with a real strict comparison `ord_compare (omega_tower n) (Gamma_0_approx (S (S n))) = Lt`.
17. Replace `worm_ordinal_embedding`, `proof_term_ordinal_in_epsilon_0`, `transfinite_worm_each_alpha_corresponds`, and `Gamma_0_bounds_predicative_strength` with comparison or order statements.

## Consolidate duplicate lemmas

18. Extract `van_benthem_forward` as the canonical witness; rewrite `van_benthem_modal_invariant`, `van_benthem_full_forward_modal_invariance`, `van_benthem_invariance_under_bisim`, `goldblatt_thomason_modal_definability_via_bisimulation`, and `categorical_logic_bisim_invariance` as one-line `Theorem _ := van_benthem_forward.` aliases.
19. Consolidate the `cut_elimination_*` cluster around `cut_admissibility` and `box_free_normalisation`.
20. Consolidate the `pi2_conservativity_*` cluster around the canonical witness.
21. Consolidate the `sambin_uniqueness_*_packaged` cluster around `fixed_point_unique_loeb_form` and `same_level_fixed_point_uniqueness`.

## Module split

22. Split `Tiling.v` into themed modules — `Calculus`, `Hilbert`, `Kripke`, `FixedPoints`, `Bew`, `ProofTerms`, `Worms`, `Agents` — keeping the dependency DAG acyclic.  Update `_CoqProject` and `Makefile`.

## Foundational infrastructure

23. Prove `ord_lt_trans`, `ord_lt_total`, `well_founded ord_lt`.  Define `wf_ord` enforcing strict CNF; prove decidability.  Restate ordinal-embedding theorems on `{ o : ord | wf_ord o }`.
24. Prove `cunpair (cpair a b) = (a, b)` and `cpair (fst (cunpair n)) (snd (cunpair n)) = n`.
25. Prove `decode_form_bounded` with depth bound `S (encode_form phi)` is a left inverse of `encode_form`; derive `decode_form (encode_form phi) = phi`.
26. Strengthen `find_root_correct` to establish it returns the genuine root of the triangle inequality.
27. Add a `Provable_with_hyp_Nec` calculus including a closed-context Necessitation rule; prove deduction theorem and cut admissibility.
28. Implement a polynomial-space tail-recursive evaluator over an explicit assignment buffer of size O(|free_vars phi|); prove the space bound and agreement with `decide_tautology`.

## Proof-term reduction

29. Extend `pt_reduces` with axiom-instance contraction rules for K, S, DN, BoxK at their argument shapes.
30. Prove confluence of the extended reduction via Newman's lemma.
31. Prove strong normalisation via a multiset-ordinal measure.

## Sambin theorems

32. Prove `forall p phi, sambin_class p phi -> exists psi, |- Iff psi (Subst p psi phi)` by induction on the `sambin_class` derivation, wiring in the existing per-class lemmas.
33. Prove fixed-point existence for `phi(p) := Neg (Box n (Var p))` (Gödel-sentence shape).
34. Prove fixed-point existence for `phi(p) := Impl (Box n (Var p)) X` for `X` not containing `p` (Henkin-sentence shape).
35. Prove fixed-point existence for `phi(p) := And X (Box n (Var p))` and other compound modalized contexts.
36. Prove the substitution composition lemma's converse direction.
37. Prove `modalized` is preserved under all calculus operations.
38. Prove the full de Jongh-Sambin uniqueness: any two fixed points of the same modalized formula are provably equivalent.

## Canonical model + Kripke completeness

39. Prove `canonical_R` satisfies transitivity from `Ax_Box4`.
40. Prove `canonical_R` satisfies monotonicity from `Ax_Mon`.
41. Prove `canonical_R` satisfies NextCon-successor from `Ax_NextCon`.
42. Prove `canonical_R` satisfies converse-well-foundedness from `Ax_Loeb`.
43. Extend the canonical-model truth lemma to the full modal language (`Box n psi` case) by structural induction on `phi`.
44. Prove Kripke completeness of GLP*: every non-theorem is refuted by some GLP*-frame.
45. Prove the finite frame property: every non-theorem is refuted by a finite GLP*-frame.
46. Prove decidability of the full polymodal calculus via filtration.
47. Prove PSPACE-completeness of GLP* satisfiability/provability.
48. Prove independence of `Ax_K` using non-Kripke (e.g. neighborhood) semantics.
49. Prove Kripke completeness for `Provable_no_NC`, `Provable_no_Mon`, `Provable_no_Loeb`, `Provable_no_B4` separately with their distinguishing frame classes.
50. Prove that each axiom-removal calculus is strictly weaker than `Provable` for infinitely many distinct theorems.

## Sequent calculus + interpolation

51. Build a sequent presentation of GLP*.
52. Prove cut admissibility structurally for the sequent calculus.
53. Prove full Craig interpolation: for every `|- Impl phi psi`, exhibit an interpolant `chi` whose free variables and Box-levels are contained in the intersection of those of `phi` and `psi`.
54. Prove Lyndon interpolation: the interpolant additionally preserves polarity of variable occurrences.
55. Prove uniform interpolation: for every `phi` and variable `p`, exhibit `phi_p` not containing `p` such that for every `psi` not containing `p`, `|- Impl phi psi <-> |- Impl phi_p psi`.
56. Prove Beth definability in its full form: every implicitly definable predicate is explicitly definable.

## Bisimulation + frame characterisation

57. Prove the unconditional reverse direction of van Benthem: every bisimulation-invariant first-order property over ω-saturated models is modally definable.
58. Strengthen `is_modal_definable` to require the witnessing formula to land in a syntactically restricted fragment matching the property's intended class; restate the bisim-invariance theorems on the strengthened predicate.
59. Prove the full Goldblatt-Thomason theorem characterizing modally-definable frame classes.
60. Prove Sahlqvist correspondence in its general form (not just for K, Löb).
61. Prove the polymodal Fine-Schurz incompleteness result identifying GLP*-formulas not derivable in any Kripke-complete sub-logic.

## Arithmetic Bew + Solovay

62. Construct a genuine arithmetic Σ₁ provability predicate `Bew_PA` over a Gödel-encoded fragment of arithmetic (formulas as numerals, proofs as numerals); prove the Hilbert-Bernays-Löb conditions for it.
63. Prove arithmetic completeness of GL (Solovay's first theorem) at every level, not just the box-free fragment.
64. Prove Solovay's second completeness theorem for the truth-extension `Provable_S` beyond the box-free fragment.
65. Prove arithmetic completeness of `Provable_GLP` (Japaridze's theorem) for arbitrary formulas, not just box-free ones.
66. Construct a genuinely non-identity, non-licensure inhabitant of `is_arithmetic_interpretation` to show the predicate has non-trivial structure beyond `identity` and `licenses k`.
67. Prove the Friedman-Sheard truth-axiomatization theorem.
68. Prove Tarski undefinability in its sharpest form: no formula `Tr(x)` in the language of GLP* with one free variable satisfies `|- Iff (Tr(phi-code)) phi` for all `phi`.

## First-order tower

69. Construct a first-order theory `T_n` with explicit axioms (not just modal axiom-schemas via `T_axiom`); prove the cumulativity, consistency, and tiling results at the genuine first-order level.
70. Prove Π₁ conservativity of `T_(n+1)` over `T_n` for arithmetic Π₁ sentences.
71. Prove Π₂ conservativity across the tower.
72. Prove Friedman's negative translation result connecting classical to constructive provability beyond the box-free case.
73. Prove the relative-consistency direction `Con(T_0) → Con(T_n)` from a strictly weaker hypothesis than meta-consistency of the full system.
74. Prove `T_no_self_consistency` directly from `Bew`'s HBL conditions rather than routing through the full Provable calculus.
75. Prove the strict separation between `Bew n` and `Bew (S n)` at the proof level (not just at the axiom-set level).
76. Prove that the structural `Bew` predicate satisfies provability logic (i.e., `Bew n` interpreted into `Box n` validates exactly GLP* at the relevant level).

## Critch + Fallenstein-Soares

77. Prove the Critch parametric bounded-Löb theorem for a genuinely bounded provability predicate (with proof-length bound encoded inside the modal formula), not just iterated Box.
78. Prove the Critch correspondence between modal `critch_bounded_box` and a genuine bounded-arithmetic provability predicate with explicit polynomial bounds.
79. Prove the Fallenstein-Soares 2014 finite-tower self-modification theorem at the arithmetic level: a `T_n`-using agent provably-safely transitions to a `T_(n+1)`-using agent.
80. Prove the Pudlák speedup result for the parametric tower at every level (super-polynomial in proof length when ascending the hierarchy).
81. Prove a quantitative version of the Löbian obstacle: bound the proof length of the inconsistency derivation by a function of the reflection-schema's proof complexity.

## Agents + cooperation

82. Define `FairBot n psi := psi := Box n (Iff (opp psi) Cooperate)` as a real Sambin fixed point where `opp` reads from the open variable; prove existence via `fixed_point_existence_top_solves` applied to the modalized self-referential formula.
83. Define `PrudentBot n psi` as a real Sambin fixed point with the consistency conjunct `Box (S n) (Neg (Box n Bot))`.
84. Prove FairBot vs FairBot mutual cooperation with the genuine fixed-point semantics.
85. Prove FairBot vs DefectBot defection.
86. Prove the BCFHLY robust-cooperation theorem for non-trivial fixed points (not just the collapse to `Top`).
87. Prove that PrudentBot strictly dominates FairBot in modal-PD against DefectBot, exhibiting concrete formula witnesses.
88. Prove the goal-preservation tiling theorem for an agent that takes non-trivial actions changing the state.
89. Prove vingean reflection in a setting where the agent's decision genuinely depends on `T_(n+1)` licensure (not vacuously satisfied by `cautious_agent`).
90. Prove the no-panic reflective-trust theorem at the level of self-modifying agents, not just modal consistency.
91. Prove the `T_kappa` agent-correspondence theorem with a non-trivial agent architecture.
92. Prove Aumann's agreement theorem in modal form: agents at different levels with common knowledge of consistency provably agree.

## Worms + ordinal analysis

93. Prove Beklemishev's worm normal form theorem for `Provable_GLP` (where worms are not all provable), not just the trivial collapse in `Provable`.
94. Prove the proof-theoretic ordinal of `Provable_GLP` is exactly ε₀ via a complete ordinal-assignment to proof terms with strict decrease under reduction.
95. Replace the syntactic `Veblen_phi_iter` and `Gamma_0_approx` shells with the genuine Veblen hierarchy as ordinal functions; prove their fixed-point properties.
96. Prove Carlson's theorem on the ordinal-analysis correspondence between worms and ordinals below ε₀.
97. Prove Carlson's theorem (second incompleteness for polymodal provability) in its sharp form.
98. Prove the explicit ε₀-rank-respecting normalization theorem for proof terms with strict ordinal decrease (currently `proof_term_ordinal` is defined but no decrease theorem under reduction is proved).

## Algebraic + categorical

99. Prove Magari algebra completeness: the Lindenbaum-Tarski algebra is the free Magari algebra on the propositional variables.
100. Prove Jónsson-Tarski duality between the Lindenbaum-Tarski algebra and the canonical frame.
101. Prove a genuine categorical-semantics theorem: define a category of GLP*-frames with bisimulation-respecting morphisms; prove `Provable` corresponds to global sections of a sheaf or similar structure.
102. Strengthen `licenses_axiomatic_uniqueness` to derive its bounds from a categorical universal property — uniqueness as the unique-up-to-provable-equivalence functor preserving `|-` and admitting `Ax_BoxK`, `Ax_Loeb`, `Ax_Mon`.
103. Prove `licenses_axiomatic_uniqueness` extends to non-extensional candidate operators via a categorical universal property.

## Quantified + temporal + probabilistic + neighborhood

104. Prove constant-domain QGLP* soundness and completeness with respect to a Kripke-style first-order semantics for quantified modal formulas.
105. Prove the Barcan and converse-Barcan formulas hold or fail in the quantified extension, with semantic witnesses.
106. Prove a genuine temporal-extension result where time and modal level interact non-trivially (e.g., temporal succession changes the proof-theoretic ordinal).
107. Prove a probabilistic-Löb theorem with a real probability parameter (not just nat) showing graded reflection survives at strictly positive ε.
108. Construct a proper neighborhood-semantics framework; prove soundness/completeness for a non-normal modal logic separating it from GLP*.

## Decision procedures + lower bounds

109. Prove decidability of admissible rules (Rybakov's theorem) for at least the box-free fragment, with a real decision procedure rather than a vacuous skeleton.
110. Prove a decidability result for the bimodal `Box n + Box m` fragment with `n ≠ m` distinct from the full polymodal case.
111. Prove the existence of a finite refuting frame for every specific non-theorem (not just `Box n Bot`).
112. Replace `lindenbaum_extend` (which uses `excluded_middle_informative`) with a constructive Lindenbaum construction over a decidable theory.
113. Prove a proper Kalmár-style completeness for the `Sigma1_modal` closure (not just box-free).

## Reflection Calculus + closed fragments

114. Prove the full Reflection Calculus completeness theorem for strictly positive formulas.
115. Prove decidability of the closed (variable-free) fragment with explicit complexity bounds via filtration.
116. Prove the Abashidze-Japaridze characterization of the closed fragment of `Provable_GLP`.
117. Prove the omega-completeness of `Fnat`: every formula true at every world of `Fnat` is provable.
118. Prove the Henkin-style truth lemma over the canonical model for the modal fragment, not just propositional cases.

## Visser interpretability

119. Prove the Visser interpretability logic ILM/ILP axioms beyond just the K-distribution and Box4 forms.
120. Prove the Visser ILM J5 axiom from the calculus axioms rather than via `Ax_Mon`.

## Disjunction + polymodal extensions

121. Prove the disjunction property for `Provable_GLP`: if `|- Or (Box n phi) (Box m psi)` then `|- Box n phi` or `|- Box m psi`.
122. Prove that `Provable_GLP_incomparable_with_provable` extends to infinitely many incomparable formulas, witnessing genuine logical separation.
123. Prove a no-go theorem for any uniform strengthening of `Ax_NextCon` to `Box n (Neg (Box n Bot))` across all levels.
124. Prove that the `Provable_plus` extension scheme yields inconsistency for any reflection-schema extension at any level uniformly.
125. Prove the reverse direction of `licensing_consistency_concrete_converse` quantifying over all formulas at level n simultaneously.
126. Prove the Smoryński bimodal independence theorem at distinct levels for non-trivial formulas.

## Conservativity + translation

127. Prove the full conservativity of `Provable_GL` over `Provable` at level 0 in both directions (currently only one direction is fully proved without using the trivial `forget_levels` collapse).
128. Prove the polymodal-fixed-point system completeness à la Smoryński.
